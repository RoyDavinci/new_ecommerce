{
    "watch": "src",
    "ext": "ts,js,json",
    "ignore": ["node_modules", "coverage", "dist"],
    "exec": "node --loader ts-node/esm  ./src/server.ts",
    "restartable": "rs",
    "env": {
        "NODE_ENV": "development"
    }
}
